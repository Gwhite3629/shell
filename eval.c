#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "eval.h"
#include "help.h"

int eval(char **tokens, char *result)
{
    int token = 0;

    token = 

    memcpy(result, line, MAX_LINE);

    return 0;
}